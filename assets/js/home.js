import data from "../settings/home.json" assert { type: "json" };



const elementHTML = document.getElementById("sidebar-profile-infos");
const contentHTML = `

`;
elementHTML.innerHTML += contentHTML;
